- name: create .bashrc
  template:
    src: "bashrc.j2"
    dest: "{{ '/root/.bashrc' if user_name == 'root' else '/home/' ~ user_name ~ '/.bashrc' }}"
    owner: "{{user_name}}"
    group: "{{user_name}}"