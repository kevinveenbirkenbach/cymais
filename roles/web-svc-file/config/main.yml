features:
  matomo:           true
  css:              true
  port-ui-desktop:  true
server:
  domains:
    canonical:
      - "file.{{ primary_domain }}"
    alias:
      - "files.{{ primary_domain }}"
