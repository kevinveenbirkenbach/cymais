version:                        "latest"
oauth2_proxy:
  application:                  application
  port:                         80
credentials:
features:
  matomo:                       true
  css:                          true
  portfolio_iframe:             true
  ldap:                         true
  central_database:             false
  oauth2:                       false
csp:
  flags:
    style-src:
      unsafe-inline:            true
    script-src:
      unsafe-inline:            true
      unsafe-eval:              true
domains:
  aliases:
    - "ldap.{{primary_domain}}"

