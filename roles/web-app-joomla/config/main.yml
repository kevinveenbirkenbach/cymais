images:
  joomla:             "joomla:latest"
features:
  matomo:             true
  css:                true
  port-ui-desktop:   true
  central_database:   true
domains:
  canonical:
    - "cms.{{ primary_domain }}"
docker:
  services:
    database:
      enabled: true