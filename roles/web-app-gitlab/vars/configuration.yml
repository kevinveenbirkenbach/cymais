images:
  gitlab:             "gitlab/gitlab-ee:latest"
features:
  matomo:             true
  css:                true
  portfolio_iframe:   true
  central_database:   true
docker:
  services:
    redis:
      enabled: true
    database: 
      enabled: true