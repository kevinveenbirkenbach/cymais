images:
  gitlab:             "gitlab/gitlab-ee:latest"
features:
  matomo:             true
  css:                true
  port-ui-desktop:   true
  central_database:   true
docker:
  services:
    redis:
      enabled: true
    database: 
      enabled: true