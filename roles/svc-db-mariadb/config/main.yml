docker:
  services:
    mariadb:
      version:          "latest"
      image:            "mariadb"
      name:             "mariadb"
      backup:
        datase_routine: true
  network:              "mariadb"
  volumes:
    data:               "mariadb_data"